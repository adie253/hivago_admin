name: Deploy to Hostinger

# Deploys ONLY when master changes — i.e. only when the CTO merges a PR.
on:
  push:
    branches: [master]

concurrency:
  group: deploy-master
  cancel-in-progress: true

jobs:
  deploy:
    # GitHub-hosted runner IPs are blocked by Hostinger's FTP firewall (control-socket
    # timeout, confirmed 2026-08-11 — connects fine from a normal residential/office IP,
    # times out from every ubuntu-latest run). Self-hosted runner on a machine with a
    # regular IP sidesteps this without waiting on Hostinger support.
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install
        run: npm install --no-audit --no-fund
      - name: Build
        run: npm run build
      - name: Deploy to Hostinger (FTPS)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          protocol: ftps
          local-dir: ./dist/
          server-dir: ${{ vars.FTP_SERVER_DIR }}
          dangerous-clean-slate: false
